{
  "name": "tiramisu-docs-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "playground"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test"
  },
  "dependencies": {
    "@timeleap/tiramisu": "^1.7.0"
  }
}
