# Tiramisu Docs

## Parser Issues

When encountering issues with the Tiramisu parser (`@timeleap/tiramisu`), report them to the user instead of inventing workarounds. The parser is maintained upstream and bugs should be fixed there.
